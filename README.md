WIP cdn that will support temporary file uploads

### `groupId` can be:

- A userId for posts/avatars/banners
- A channelId for messages

`POST /attachments/:groupId`

`POST /banners/:groupId`

`POST /avatars/:groupId`

`POST /emojis`
